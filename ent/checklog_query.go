// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/compscore/compscore/ent/check"
	"github.com/compscore/compscore/ent/checklog"
	"github.com/compscore/compscore/ent/predicate"
	"github.com/compscore/compscore/ent/round"
	"github.com/compscore/compscore/ent/team"
)

// CheckLogQuery is the builder for querying CheckLog entities.
type CheckLogQuery struct {
	config
	ctx        *QueryContext
	order      []checklog.OrderOption
	inters     []Interceptor
	predicates []predicate.CheckLog
	withCheck  *CheckQuery
	withTeam   *TeamQuery
	withRound  *RoundQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CheckLogQuery builder.
func (clq *CheckLogQuery) Where(ps ...predicate.CheckLog) *CheckLogQuery {
	clq.predicates = append(clq.predicates, ps...)
	return clq
}

// Limit the number of records to be returned by this query.
func (clq *CheckLogQuery) Limit(limit int) *CheckLogQuery {
	clq.ctx.Limit = &limit
	return clq
}

// Offset to start from.
func (clq *CheckLogQuery) Offset(offset int) *CheckLogQuery {
	clq.ctx.Offset = &offset
	return clq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (clq *CheckLogQuery) Unique(unique bool) *CheckLogQuery {
	clq.ctx.Unique = &unique
	return clq
}

// Order specifies how the records should be ordered.
func (clq *CheckLogQuery) Order(o ...checklog.OrderOption) *CheckLogQuery {
	clq.order = append(clq.order, o...)
	return clq
}

// QueryCheck chains the current query on the "check" edge.
func (clq *CheckLogQuery) QueryCheck() *CheckQuery {
	query := (&CheckClient{config: clq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := clq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := clq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(checklog.Table, checklog.FieldID, selector),
			sqlgraph.To(check.Table, check.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, checklog.CheckTable, checklog.CheckColumn),
		)
		fromU = sqlgraph.SetNeighbors(clq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTeam chains the current query on the "team" edge.
func (clq *CheckLogQuery) QueryTeam() *TeamQuery {
	query := (&TeamClient{config: clq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := clq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := clq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(checklog.Table, checklog.FieldID, selector),
			sqlgraph.To(team.Table, team.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, checklog.TeamTable, checklog.TeamColumn),
		)
		fromU = sqlgraph.SetNeighbors(clq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRound chains the current query on the "round" edge.
func (clq *CheckLogQuery) QueryRound() *RoundQuery {
	query := (&RoundClient{config: clq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := clq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := clq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(checklog.Table, checklog.FieldID, selector),
			sqlgraph.To(round.Table, round.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, checklog.RoundTable, checklog.RoundColumn),
		)
		fromU = sqlgraph.SetNeighbors(clq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CheckLog entity from the query.
// Returns a *NotFoundError when no CheckLog was found.
func (clq *CheckLogQuery) First(ctx context.Context) (*CheckLog, error) {
	nodes, err := clq.Limit(1).All(setContextOp(ctx, clq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{checklog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (clq *CheckLogQuery) FirstX(ctx context.Context) *CheckLog {
	node, err := clq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CheckLog ID from the query.
// Returns a *NotFoundError when no CheckLog ID was found.
func (clq *CheckLogQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = clq.Limit(1).IDs(setContextOp(ctx, clq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{checklog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (clq *CheckLogQuery) FirstIDX(ctx context.Context) int {
	id, err := clq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CheckLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CheckLog entity is found.
// Returns a *NotFoundError when no CheckLog entities are found.
func (clq *CheckLogQuery) Only(ctx context.Context) (*CheckLog, error) {
	nodes, err := clq.Limit(2).All(setContextOp(ctx, clq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{checklog.Label}
	default:
		return nil, &NotSingularError{checklog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (clq *CheckLogQuery) OnlyX(ctx context.Context) *CheckLog {
	node, err := clq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CheckLog ID in the query.
// Returns a *NotSingularError when more than one CheckLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (clq *CheckLogQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = clq.Limit(2).IDs(setContextOp(ctx, clq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{checklog.Label}
	default:
		err = &NotSingularError{checklog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (clq *CheckLogQuery) OnlyIDX(ctx context.Context) int {
	id, err := clq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CheckLogs.
func (clq *CheckLogQuery) All(ctx context.Context) ([]*CheckLog, error) {
	ctx = setContextOp(ctx, clq.ctx, "All")
	if err := clq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CheckLog, *CheckLogQuery]()
	return withInterceptors[[]*CheckLog](ctx, clq, qr, clq.inters)
}

// AllX is like All, but panics if an error occurs.
func (clq *CheckLogQuery) AllX(ctx context.Context) []*CheckLog {
	nodes, err := clq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CheckLog IDs.
func (clq *CheckLogQuery) IDs(ctx context.Context) (ids []int, err error) {
	if clq.ctx.Unique == nil && clq.path != nil {
		clq.Unique(true)
	}
	ctx = setContextOp(ctx, clq.ctx, "IDs")
	if err = clq.Select(checklog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (clq *CheckLogQuery) IDsX(ctx context.Context) []int {
	ids, err := clq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (clq *CheckLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, clq.ctx, "Count")
	if err := clq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, clq, querierCount[*CheckLogQuery](), clq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (clq *CheckLogQuery) CountX(ctx context.Context) int {
	count, err := clq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (clq *CheckLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, clq.ctx, "Exist")
	switch _, err := clq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (clq *CheckLogQuery) ExistX(ctx context.Context) bool {
	exist, err := clq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CheckLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (clq *CheckLogQuery) Clone() *CheckLogQuery {
	if clq == nil {
		return nil
	}
	return &CheckLogQuery{
		config:     clq.config,
		ctx:        clq.ctx.Clone(),
		order:      append([]checklog.OrderOption{}, clq.order...),
		inters:     append([]Interceptor{}, clq.inters...),
		predicates: append([]predicate.CheckLog{}, clq.predicates...),
		withCheck:  clq.withCheck.Clone(),
		withTeam:   clq.withTeam.Clone(),
		withRound:  clq.withRound.Clone(),
		// clone intermediate query.
		sql:  clq.sql.Clone(),
		path: clq.path,
	}
}

// WithCheck tells the query-builder to eager-load the nodes that are connected to
// the "check" edge. The optional arguments are used to configure the query builder of the edge.
func (clq *CheckLogQuery) WithCheck(opts ...func(*CheckQuery)) *CheckLogQuery {
	query := (&CheckClient{config: clq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	clq.withCheck = query
	return clq
}

// WithTeam tells the query-builder to eager-load the nodes that are connected to
// the "team" edge. The optional arguments are used to configure the query builder of the edge.
func (clq *CheckLogQuery) WithTeam(opts ...func(*TeamQuery)) *CheckLogQuery {
	query := (&TeamClient{config: clq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	clq.withTeam = query
	return clq
}

// WithRound tells the query-builder to eager-load the nodes that are connected to
// the "round" edge. The optional arguments are used to configure the query builder of the edge.
func (clq *CheckLogQuery) WithRound(opts ...func(*RoundQuery)) *CheckLogQuery {
	query := (&RoundClient{config: clq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	clq.withRound = query
	return clq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Error string `json:"error,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CheckLog.Query().
//		GroupBy(checklog.FieldError).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (clq *CheckLogQuery) GroupBy(field string, fields ...string) *CheckLogGroupBy {
	clq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CheckLogGroupBy{build: clq}
	grbuild.flds = &clq.ctx.Fields
	grbuild.label = checklog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Error string `json:"error,omitempty"`
//	}
//
//	client.CheckLog.Query().
//		Select(checklog.FieldError).
//		Scan(ctx, &v)
func (clq *CheckLogQuery) Select(fields ...string) *CheckLogSelect {
	clq.ctx.Fields = append(clq.ctx.Fields, fields...)
	sbuild := &CheckLogSelect{CheckLogQuery: clq}
	sbuild.label = checklog.Label
	sbuild.flds, sbuild.scan = &clq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CheckLogSelect configured with the given aggregations.
func (clq *CheckLogQuery) Aggregate(fns ...AggregateFunc) *CheckLogSelect {
	return clq.Select().Aggregate(fns...)
}

func (clq *CheckLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range clq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, clq); err != nil {
				return err
			}
		}
	}
	for _, f := range clq.ctx.Fields {
		if !checklog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if clq.path != nil {
		prev, err := clq.path(ctx)
		if err != nil {
			return err
		}
		clq.sql = prev
	}
	return nil
}

func (clq *CheckLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CheckLog, error) {
	var (
		nodes       = []*CheckLog{}
		withFKs     = clq.withFKs
		_spec       = clq.querySpec()
		loadedTypes = [3]bool{
			clq.withCheck != nil,
			clq.withTeam != nil,
			clq.withRound != nil,
		}
	)
	if clq.withCheck != nil || clq.withTeam != nil || clq.withRound != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, checklog.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CheckLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CheckLog{config: clq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, clq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := clq.withCheck; query != nil {
		if err := clq.loadCheck(ctx, query, nodes, nil,
			func(n *CheckLog, e *Check) { n.Edges.Check = e }); err != nil {
			return nil, err
		}
	}
	if query := clq.withTeam; query != nil {
		if err := clq.loadTeam(ctx, query, nodes, nil,
			func(n *CheckLog, e *Team) { n.Edges.Team = e }); err != nil {
			return nil, err
		}
	}
	if query := clq.withRound; query != nil {
		if err := clq.loadRound(ctx, query, nodes, nil,
			func(n *CheckLog, e *Round) { n.Edges.Round = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (clq *CheckLogQuery) loadCheck(ctx context.Context, query *CheckQuery, nodes []*CheckLog, init func(*CheckLog), assign func(*CheckLog, *Check)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CheckLog)
	for i := range nodes {
		if nodes[i].check_log_check == nil {
			continue
		}
		fk := *nodes[i].check_log_check
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(check.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "check_log_check" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (clq *CheckLogQuery) loadTeam(ctx context.Context, query *TeamQuery, nodes []*CheckLog, init func(*CheckLog), assign func(*CheckLog, *Team)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CheckLog)
	for i := range nodes {
		if nodes[i].check_log_team == nil {
			continue
		}
		fk := *nodes[i].check_log_team
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(team.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "check_log_team" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (clq *CheckLogQuery) loadRound(ctx context.Context, query *RoundQuery, nodes []*CheckLog, init func(*CheckLog), assign func(*CheckLog, *Round)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CheckLog)
	for i := range nodes {
		if nodes[i].check_log_round == nil {
			continue
		}
		fk := *nodes[i].check_log_round
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(round.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "check_log_round" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (clq *CheckLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := clq.querySpec()
	_spec.Node.Columns = clq.ctx.Fields
	if len(clq.ctx.Fields) > 0 {
		_spec.Unique = clq.ctx.Unique != nil && *clq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, clq.driver, _spec)
}

func (clq *CheckLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(checklog.Table, checklog.Columns, sqlgraph.NewFieldSpec(checklog.FieldID, field.TypeInt))
	_spec.From = clq.sql
	if unique := clq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if clq.path != nil {
		_spec.Unique = true
	}
	if fields := clq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, checklog.FieldID)
		for i := range fields {
			if fields[i] != checklog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := clq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := clq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := clq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := clq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (clq *CheckLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(clq.driver.Dialect())
	t1 := builder.Table(checklog.Table)
	columns := clq.ctx.Fields
	if len(columns) == 0 {
		columns = checklog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if clq.sql != nil {
		selector = clq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if clq.ctx.Unique != nil && *clq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range clq.predicates {
		p(selector)
	}
	for _, p := range clq.order {
		p(selector)
	}
	if offset := clq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := clq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CheckLogGroupBy is the group-by builder for CheckLog entities.
type CheckLogGroupBy struct {
	selector
	build *CheckLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (clgb *CheckLogGroupBy) Aggregate(fns ...AggregateFunc) *CheckLogGroupBy {
	clgb.fns = append(clgb.fns, fns...)
	return clgb
}

// Scan applies the selector query and scans the result into the given value.
func (clgb *CheckLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, clgb.build.ctx, "GroupBy")
	if err := clgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CheckLogQuery, *CheckLogGroupBy](ctx, clgb.build, clgb, clgb.build.inters, v)
}

func (clgb *CheckLogGroupBy) sqlScan(ctx context.Context, root *CheckLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(clgb.fns))
	for _, fn := range clgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*clgb.flds)+len(clgb.fns))
		for _, f := range *clgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*clgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := clgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CheckLogSelect is the builder for selecting fields of CheckLog entities.
type CheckLogSelect struct {
	*CheckLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cls *CheckLogSelect) Aggregate(fns ...AggregateFunc) *CheckLogSelect {
	cls.fns = append(cls.fns, fns...)
	return cls
}

// Scan applies the selector query and scans the result into the given value.
func (cls *CheckLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cls.ctx, "Select")
	if err := cls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CheckLogQuery, *CheckLogSelect](ctx, cls.CheckLogQuery, cls, cls.inters, v)
}

func (cls *CheckLogSelect) sqlScan(ctx context.Context, root *CheckLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cls.fns))
	for _, fn := range cls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
